**1.1.  В папке /USR/LOCAL/ создать 2 директории: folder_max, folder_min**

```bash
sudo mkdir  /usr/local/folder_max 

sudo mkdir  /usr/local/folder_min
```

1.2.  Создать 2-х группы пользователей: group_max, group_min

```bash
 sudo groupadd group_max

sudo groupadd group_min
```
1.3.  Создать 2-х пользователей: user_max_1, user_min_1

```bash
sudo useradd -m -G group_max user_max_1
sudo useradd -m -G group_min user_min_1
sudo usermod -aG group_min user_max_1
```

1.4 Для пользователей из группы *_max дать полный доступ на директории *_max и *_min. Для пользователей группы *_min дать полный доступ только на директорию *_min

```bash
sudo chown :group_max /usr/local/folder_max

sudo chmod 770 /usr/local/folder_max


sudo chown :group_min /usr/local/folder_min

sudo chmod 770 /usr/local/folder_min



```

1.5.  Создать и исполнить (пользователем из той же категории) скрипт в директории folder_max, который пишет текущую дату/время в файл output.log в текущей директории

```bash

sudo -u user_max_1 bash -c 'echo "date > /usr/local/folder_max/output.log" > /usr/local/folder_max/script1.sh'


sudo -u user_max_1 chmod +x /usr/local/folder_max/script1.sh


sudo -u user_max_1 /usr/local/folder_max/script1.sh
```
<img width="512" height="36" alt="image" src="https://github.com/user-attachments/assets/6baacff1-4940-45c4-8361-4c8668b79d27" />

1.6.  Создать и исполнить (пользователем из той же категории) скрипт в директории folder_max, который пишет текущую дату/время в файл output.log в директории *_min

```bash



sudo -u user_max_1 bash -c 'echo "date > /usr/local/folder_min/output.log" > /usr/local/folder_max/script2.sh'


sudo -u user_max_1 chmod +x /usr/local/folder_max/script2.sh


sudo -u user_max_1 /usr/local/folder_max/script2.sh
```
<img width="497" height="53" alt="image" src="https://github.com/user-attachments/assets/c36f1d9a-a501-4f64-9845-923ae137c4a3" />


1.7.  Исполнить (пользователем *_min) скрипт в директории folder_max, который пишет текущую дату/время в файл output.log в директории *_min

```bash

sudo -u user_min_1 /usr/local/folder_max/script2.sh.log

```

Permission denied

С другой стороны можно выполнить пользователем max , он же в группе мин, тогда все ок.

```bash
 sudo -u user_max_1 /usr/local/folder_max/script2.sh
```
<img width="238" height="26" alt="image" src="https://github.com/user-attachments/assets/f286d888-48b3-49e0-aa1d-42471c3b8a19" />



1.81.1.  Создать и исполнить (пользователем из той же категории) скрипт в директории folder_min, который пишет текущую дату/время в файл output.log в директории *_max
```bash
# Создаем скрипт
sudo -u user_min_1 bash -c 'echo "date > /usr/local/folder_max/output_min.log" > /usr/local/folder_min/script3.sh'


sudo -u user_min_1 chmod +x /usr/local/folder_min/script3.sh


sudo -u user_min_1 /usr/local/folder_min/script3.sh
```
Если так выполнить , то файл запишется но НЕ ИСПОЛНИТСЯ.

Опять таки, воспользовался лайфхаком

<img width="247" height="35" alt="image" src="https://github.com/user-attachments/assets/1343b881-8007-428e-b13a-517747152d43" />




1.9
Вывести перечень прав доступа у папок *_min/ *_max, а также у всего содержимого внутри

```bash
ls -lR /usr/local/folder_max /usr/local/folder_min
```
<img width="541" height="211" alt="image" src="https://github.com/user-attachments/assets/4a181cc8-5711-4a28-876b-e7d70bd0d21c" />


2 Docker

2.1.  Создать скрипт, который пишет текущую дату/время в файл output.log в текущей директории

```bash
mkdir docker_task 
cd docker_task 



cat << 'EOF' > script.sh
#!/bin/bash
date >> output.log
echo "Дата и время записаны в output.log"
EOF


chmod +x script.sh
```
```bash


cat << 'EOF' > script.sh
#!/bin/bash
date >> output.log
echo "Дата и время записаны в output.log"
EOF

# Делаем файл исполняемым
chmod +x script.sh
```
2.2 Собрать образ со скриптами выше и с пакетом nano (docker build)

```Dockerfile


cat << EOF > Dockerfile

FROM ubuntu:latest 
RUN apt-get update && apt-get install -y nano
COPY script.sh .


RUN chmod +x script.sh

EOF
```


2.3.  Запустить образ (docker run)
```bash

docker build -t lab1_dock .
```

2.4
```bash
docker run lab1_dock
```

2.5
```bash
docker run --rm lab1_dock cat /etc/passwd
```
<img width="502" height="306" alt="image" src="https://github.com/user-attachments/assets/0f6181fe-553f-4296-80ea-b0c5e156abb0" />

3 .  GitHubGitLab, в котором будут содержаться все выполненные ЛР
3.1   Создать репозиторий в GitHub или GitLab
```bash
mkdir lab1_cpp
cd lab1_cpp

```
3.2 1.1.  Создать структуру репозитория:
```bash
 mkdir -p lab_{01,02}/{build,src,doc,cmake}
```
```bash

nano dev_to_stg.sh

#!/bin/bash
git checkout stg
git merge dev --no-ff -m "Merge dev to stg $(date +'%Y-%m-%d %H:%M:%S')"
TAG_NAME="stg-$(date +'%Y%m%d_%H%M%S')"
git tag $TAG_NAME
git push origin stg --tags
git checkout dev
```
```bash
nano dev_to_stg.sh


#!/bin/bash
git checkout stg
git merge dev --no-ff -m "Merge dev to stg $(date +'%Y-%m-%d %H:%M:%S')"
TAG_NAME="stg-$(date +'%Y%m%d_%H%M%S')"
git tag $TAG_NAME
git push origin stg --tags
git checkout dev
```

```bash
chmod +x dev_to_stg.sh stg_to_prd.sh
```
